#include "attributeinfo.h"

ATTRIBUTE_INFO** initATTRIBUTE_INFO() {
	ATTRIBUTE_INFO** toReturn = (ATTRIBUTE_INFO**)malloc(sizeof(ATTRIBUTE_INFO));
	*(toReturn) = (ATTRIBUTE_INFO*)malloc(sizeof(ATTRIBUTE_INFO));

	// TODO

	return toReturn;
}